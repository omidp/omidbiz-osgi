package org.omidbiz.payment;

import org.omidbiz.user.UserAPI;
import org.osgi.framework.BundleActivator;
import org.osgi.framework.BundleContext;
import org.osgi.framework.ServiceReference;

public class Activator implements BundleActivator {

	private static BundleContext context;

	static BundleContext getContext() {
		return context;
	}

	/*
	 * (non-Javadoc)
	 * @see org.osgi.framework.BundleActivator#start(org.osgi.framework.BundleContext)
	 */
	public void start(BundleContext bundleContext) throws Exception {
		Activator.context = bundleContext;
		ServiceReference<UserAPI> serviceReference = bundleContext.getServiceReference(UserAPI.class);
		System.out.println("user service ref is null " + serviceReference == null);
		if(serviceReference != null)
		{
			UserAPI service = bundleContext.getService(serviceReference);
			System.out.println(service.getUserList());
		}
	}

	/*
	 * (non-Javadoc)
	 * @see org.osgi.framework.BundleActivator#stop(org.osgi.framework.BundleContext)
	 */
	public void stop(BundleContext bundleContext) throws Exception {
		Activator.context = null;
	}

}
